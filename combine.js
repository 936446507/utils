/**
 * @param {number} n
 * @param {number} k
 * @return {number[][]}
 */
var combine = function (n, k) {
  const result = [];
  if (n < k) return result;

  const end = n.length - k;
  let curIndex = 0;
  for (let i = curIndex; i < end; i++) {

  }
};